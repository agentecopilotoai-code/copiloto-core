global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yaml

scrape_configs:
  - job_name: copilotoia-core
    metrics_path: /metrics
    static_configs:
      - targets: ['api:8000']
